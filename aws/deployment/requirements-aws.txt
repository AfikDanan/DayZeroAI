# AWS Lambda optimized requirements
# Minimal dependencies for production deployment

# Core Framework (lighter versions for Lambda)
fastapi==0.104.1
pydantic==2.5.0
pydantic-settings==2.1.0

# AWS SDK
boto3==1.34.0
botocore==1.34.0

# Redis (for ElastiCache)
redis==5.0.1

# AI Services
openai==1.3.0

# Image/Video Processing (Lambda optimized)
Pillow==10.1.0

# Audio Processing
pydub==0.25.1

# Email (keep SendGrid as backup)
sendgrid==6.10.0

# HTTP Client
httpx==0.25.0

# Utilities
python-multipart==0.0.6

# Lambda-specific
mangum==0.17.0  # ASGI adapter for Lambda

# Google Cloud TTS (if needed)
google-cloud-texttospeech==2.14.1